pub struct User {
    pub(crate) id: String,
    pub(crate) name: String,
    pub(crate) username: String,
    pub(crate) password: String,
    pub(crate) privilege: String
}

pub struct Product {
    id: String,
    name: String,
}